TESTING:
    - AUTH / LOGIN
        . register -> working
        . login -> working
        . two-factor -> working
        . cookie -> working
        . oauth -> working

    - CHAT
        . general messages -> working
        . general buttons -> working (except invite to play)
        . private messages -> working

        // Rooms
            . se, nel form di creazione della room, si preme invio invece di cliccare su submit, la creazione non ha luogo,
                per come la vedo si può ignorare il problema. Fammi sapere che ne pensi

            .privateRoom -> gestire le notifiche

    - USER ACCOUNT
        . friend request -> working
        . notification -> working
        . block / unblock -> working
        . settings -> all working
        profile page di amico -> se si rimuove l'amico la pagina deve ricaricarsi

    - GAME
        . general -> working
        . matches -> working
        . private game -> al momento viene inviata una chiamata socket 'customGame' con un oggetto con seguenti campi: {
            user: string = opponent,
            map: string = path dell'immagine,
            points: string = punteggio per la vittoria
        }
            - da gestire l'abbandono della partita mentre si attende risposta dell'opponent
        . achievement -> to implement
        . leaderboard -> working


    - REMOTE RUNNING
        . quando si esegue la web app su un server esterno rispetto al client di riferimento, modificare nei seguenti punti
            1. Settings dell'API della 42
            2. Link "login to ft_transcendence with 42 intra" in Login.svelte
            3. In caso cambiare il secret in GetToken.svelte 

    - TODO
        . aggiungere da frontend status ingame

    - ho aggiunto una chiamata api per avere tutte le chat, solo per testare. Ho anche aggiunto una chiamata per avere la conversazione fra 2 utenti ordinata in ordine crescente (dal primo all'ultimo);
    la chiamata e': localhost:3000/chat/"author"/"dest";