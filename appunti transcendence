TESTING:
    - AUTH / LOGIN
        . register -> working
        . login -> working
        . two-factor -> working
        . cookie -> working
        . oauth -> working

    - CHAT
        . general messages -> working
        . general buttons -> working (except invite to play)
        . private messages -> working

        // Rooms
            . se, nel form di creazione della room, si preme invio invece di cliccare su submit, la creazione non ha luogo,
                per come la vedo si può ignorare il problema. Fammi sapere che ne pensi

            . promote/demote -> working (quando si diventa admin viene ricaricata solo la sezione della chat  e, in alto, la rooom non appare tra "your rooms" ma in "rooms available" fino al prossimo ricaricamento della sezione room. Ignorabile, secondo me)

            .c'è da aggiungere un messaggio websocket (da backend a frontend) su 'roomsChanged', vuoto, che sia inviato a tutti gli utenti connessi attualmente, e deve essere inviato ogni volta che viene creata una nuova room o ne viene eliminata un'altra, oppure deve essere inviata esclusivamente all'utente oggetto di promote e demote admin (per risolvere il relativo problema di cui sopra);

    - USER ACCOUNT
        . friend request -> working
        . notification -> working
        . block / unblock -> working
        . settings -> all working

    - GAME
        . general -> working
        . matches -> working
        . private game -> to implement
        . achievement -> to implement
        . leaderboard -> working


    - REMOTE RUNNING
        . quando si esegue la web app su un server esterno rispetto al client di riferimento, modificare nei seguenti punti
            1. Settings dell'API della 42
            2. Link "login to ft_transcendence with 42 intra" in Login.svelte
            3. In caso cambiare il secret in GetToken.svelte 

    - TODO
        . quando si crea una stanza, bisogna inviare anche il tipo di policy
        . policy accettate: "PUBLIC", "PROTECTED", "PRIVATE"

    - ho aggiunto una chiamata api per avere tutte le chat, solo per testare. Ho anche aggiunto una chiamata per avere la conversazione fra 2 utenti ordinata in ordine crescente (dal primo all'ultimo);
    la chiamata e': localhost:3000/chat/"author"/"dest";