TESTING:
    - AUTH / LOGIN
        . register -> working
        . login -> working
        . two-factor -> working BUT : when disabling twofa, frontend displays new qr code ---
                                        it's a problem for backend too, because Google Authenticator creates new code for same user@email
                                        and the correct one is the latest
        . cookie -> working
        . oauth -> working

    - CHAT
        . general messages -> working
        . general buttons -> working (except invite to play)
        . private messages not displayed if not open chat

        // Rooms
            . Errore nel leaveRoom di una protected room -> il backend dà questo errore (anche se l'utente è effettivamente cancellato dal campo members)
            {
                backend     | [Nest] 158  - 06/28/2023, 2:10:35 PM   ERROR [WsExceptionsHandler] data and hash arguments required
                backend     | Error: data and hash arguments required
                backend     |     at Object.compareSync (/usr/src/backend/node_modules/bcrypt/bcrypt.js:166:15)
                backend     |     at comparePassword (/usr/src/backend/src/utils/bcrypt.ts:10:19)
                backend     |     at ChatGateway.<anonymous> (/usr/src/backend/src/chat/chat.gateway.ts:309:29)
                backend     |     at Generator.next (<anonymous>)
                backend     |     at fulfilled (/usr/src/backend/dist/src/chat/chat.gateway.js:14:58)
            }
            . se, nel form di creazione della room, si preme invio invece di cliccare su submit, la creazione non ha luogo,
                per come la vedo si può ignorare il problema. Fammi sapere che ne pensi


    - USER ACCOUNT
        . friend request -> working
        . notification -> working and dismissing correctly
        . block / unblock -> working
        . settings -> all working

    - GAME
        . general -> working
        . matches -> working
        . private game -> to implement
        . achievement -> to implement
        . leaderboard -> working
